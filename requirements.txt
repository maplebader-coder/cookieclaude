# Claude 第三方管理客户端 v2.0 - Python 依赖
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
aiosqlite==0.20.0
pydantic==2.7.1
python-multipart==0.0.9
python-dotenv==1.0.1

# 可选：mitmproxy Cookie 捕获方案
# mitmproxy==10.3.1

# 可选：Playwright 浏览器自动化登录（Cookie 自动刷新必需）
# playwright==1.44.0
# 安装后执行: playwright install chromium
